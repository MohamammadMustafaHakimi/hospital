<h1>This is the show file and shows a single doctor</h1>
<div>
  <li><%= "#{@doctor.title}.#{@doctor.first_name} #{@doctor.last_name.capitalize}" %></li>
</div>

<%= link_to "Edit", edit_doctor_path %>
<%= link_to "Delete", doctor_path(@doctor), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
